<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="create_database">
        CREATE TABLE `timers` (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            time INTEGER,
            buzzType INTEGER,
            repeatMode INTEGER,
            timestamp INTEGER,
            state TEXT
        );
    </string>
</resources>
